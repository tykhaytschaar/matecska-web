/// <reference types="vite/client" />

/** Fordításkor beinjektált értékek (vite.config.ts `define`). */
declare const __APP_VERSION__: string;
/** ISO 8601 időbélyeg a build pillanatáról. */
declare const __BUILD_TIME__: string;
